# This script stores some potentially useful constants

# ==============================================================================
# UiO colors (source: https://www.uio.no/om/designmanual/grunnelementer/farger/)
# ==============================================================================
uiored <- rgb(1, 0, 0) # i.e. just regular red
uioblack <- rgb(0, 0, 0) # i.e. just regular black
uiogray <- rgb(181, 172, 163, maxColorValue = 255) # now we're talking!
# TODO: load this with package
# TODO: either make it hidden or accessible with quotes (like regular colors)